const validateBody = require("./validation");
const validateCreateBody = require("./validationCreateBody");
const isValidId = require("./isValidId");

module.exports = { validateBody, validateCreateBody, isValidId };
